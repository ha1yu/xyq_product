# 更新日志

## [1.4.0] - 2026-05-24

### 新增

- **Excel 导入按钮** — 商品管理页新增"Excel导入"按钮，支持上传 .xlsx 文件导入商品和价格
- **Excel 导入接口** — POST `/api/import`，接收 multipart 文件上传，自动解析日期列和价格，事务导入
- **ExcelImportDialog 组件** — 拖拽上传弹窗，导入完成后显示统计（新增商品数、导入价格数）

### 修复

- **Excel 日期解析** — 支持 `MM-DD-YY` 格式的日期表头（如 `01-01-21`），兼容 Excel 序列号格式

### 变更

- **构建脚本简化** — `build.sh` 移除数据库初始化步骤（首次运行 `./xyq_product` 时自动初始化）

## [1.3.0] - 2026-05-23

### 新增

- **系统设置菜单** — 右上角下拉菜单新增"系统设置"入口
- **OCR 配置管理** — 在设置弹窗中配置 OCR 服务地址和模型名称，数据保存至数据库，OCR 请求动态读取
- **修改管理员密码** — 设置弹窗支持修改密码（验证旧密码），修改成功后自动跳转登录页
- **settings 数据表** — 新增 `settings` 表（key-value），存储系统配置

### 修复

- **卡片价格显示换行** — 价格数值四舍五入到小数点后2位，添加 `white-space: nowrap` 防止换行

### 移除

- **Excel 导入功能** — 移除 `--xlsx` 和 `--init-db` 命令行参数，删除 `seed.go`，管理员和分类初始化改为启动时自动执行

## [1.2.0] - 2026-05-23

### 新增

- **价格总览卡片布局** — 价格总览页由表格改为卡片网格布局，支持悬浮交互效果
- **点击查看走势弹窗** — 点击商品卡片弹出价格走势图（ECharts 折线图），展示统计信息（记录数/均价/最低/最高）
- **加载状态与空状态** — 数据加载时显示 loading，无数据/无搜索结果时展示对应空状态提示

### 修复

- **走势图纵轴单位缺失** — 调整 yAxis nameGap 使"万两"单位正常显示
- **走势图日期裁剪** — 增大图表 grid 右侧和底部边距，防止日期标签被遮挡
- **统计栏文字换行** — 统计数值添加 `white-space: nowrap` 防止"万两"等文字截断

## [1.1.0] - 2026-05-23

### 修复

- **OCR 解析失败** — AI 模型返回的 JSON 缺少逗号和闭合 `]`，重写解析逻辑为逐对象提取，不再依赖数组完整性
- **OCR 导入按钮不可点击** — 未匹配商品自动创建并录入价格，新增分类选择器
- **OCR 价格单位错误** — 游戏截图单位是"两"，除以 10000 转换为系统单位"万两"
- **价格总览只展示1次价格** — 改为展示最近3次价格（含日期）

### 新增

- **商品名唯一约束** — `products.name` 加 UNIQUE 约束，同名商品无法重复创建
- **重复创建自动兜底** — 创建商品接口检测重名时返回已有 ID，OCR 导入自动复用

## [1.0.0] - 2026-05-19

### 新增

- **价格总览** — 按分类 Tab 展示所有商品最新价格，支持关键词搜索
- **商品管理** — 商品增删改查，支持分类筛选和价格记录管理
- **走势分析** — ECharts 折线图展示价格趋势，支持多商品对比
- **管理员登录** — JWT 认证 (admin/123456)，游客可浏览，管理员可编辑
- **Excel 数据导入** — 从《梦幻将军物价表.xlsx》导入 132 个商品、7 个日期列的价格数据
- **一键构建** — `build.sh` 自动构建前端 + 后端 + 初始化数据库
- **自动化测试** — `test.sh` 覆盖 14 项 API 测试（登录/CRUD/权限/走势/前端）

### 技术栈

- 后端: Go + SQLite (modernc.org/sqlite, 纯Go无CGO) + JWT + bcrypt
- 前端: Vue3 + Element Plus + ECharts + Pinia + Axios + Vite
- 部署: 前端构建产物嵌入 Go 二进制，单文件部署，监听 `0.0.0.0:8081`
